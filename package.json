{
  "name": "lorne-orbit",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-frontend.js",
    "check": "node --check worker/index.mjs && node --check scripts/build-frontend.js && node --check scripts/hash-worker-password.js && node --check scripts/postgres-to-d1-sql.js && node --check scripts/backup-to-d1-sql.js && node --check scripts/smoke-worker.js",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "smoke": "node scripts/smoke-worker.js",
    "hash-password": "node scripts/hash-worker-password.js",
    "export:d1": "node scripts/postgres-to-d1-sql.js",
    "import:backup": "node scripts/backup-to-d1-sql.js"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.33.1"
  },
  "engines": {
    "node": ">=20"
  }
}
