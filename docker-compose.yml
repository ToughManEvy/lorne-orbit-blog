services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: lorne_orbit
      POSTGRES_USER: blog
      POSTGRES_PASSWORD: blog
    ports:
      - "5432:5432"
    volumes:
      - blog_postgres_data:/var/lib/postgresql/data

volumes:
  blog_postgres_data:
